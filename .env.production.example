# Production environment — copy to /etc/exam-ready.env on server
NODE_ENV=production
PORT=3847

# Database (system Postgres)
DATABASE_URL=postgresql://examready:CHANGE_ME@localhost:5432/examready

# Auth (generate with: openssl rand -base64 32)
SESSION_SECRET=replace_with_32_byte_random_secret
